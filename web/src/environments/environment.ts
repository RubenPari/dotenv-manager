/**
 * Web environment (development)
 * @module web/environments/environment
 * @description Build-time environment configuration for development.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api/v1',
};
