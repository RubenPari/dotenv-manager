/**
 * Web environment (production)
 * @module web/environments/environment.production
 * @description Build-time environment configuration for production.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
};
